# AI-Driven Finance Analyzer — combined dependencies for all modules
# Install with: pip install -r requirements.txt

# ── Data collection / ingestion ────────────────────
pypdf>=4.0.0
langchain-text-splitters>=0.2.0
pandas>=2.0.0

# ── Embeddings & FAISS vector DB ───────────────────
sentence-transformers>=2.6.0   # BAAI/bge-small-en-v1.5 embedding model
faiss-cpu>=1.7.4
numpy>=1.24.0

# ── LLM integration (retriever + generation) ───────
requests>=2.31.0               # Ollama HTTP API + OpenAI HTTP API
python-dotenv>=1.0.0           # Optional: load OPENAI_API_KEY from .env file

# ── Streamlit frontend ──────────────────────────────
streamlit>=1.35.0

# ── Testing ────────────────────────────────────────────────────
# (unittest is built into Python — no extra install needed)

# Notes:
# - Ollama (local LLM server) is installed separately, not via pip:
#     macOS/Linux: curl -fsSL https://ollama.com/install.sh | sh
#     Windows    : https://ollama.com/download
#     Pull models: ollama pull llama3
#                  ollama pull mistral
